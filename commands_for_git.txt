git config --global user.name "name"
git config --global user.email "email"

git init

git status

git add *.cpp
git add .

git commit -m "comment"

git remote add origin https://github.com/WaldemarKarasev/engine.git
git branch -M main
git push -u origin main